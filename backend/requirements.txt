# Core
Django==5.2
djangorestframework==3.16.0
django-cors-headers==4.7.0
django-filter==25.1
psycopg2-binary==2.9.10
gunicorn==21.2.0
python-dotenv==1.0.1

# Soulseek (slskd API client)
requests==2.32.3

# Matching engine
rapidfuzz==3.11.0

# Track identification
pyacoustid==1.3.0

# Audio metadata
mutagen==1.47.0

# Scraping
beautifulsoup4==4.12.3
lxml==5.3.0

# Imports (Wanted List)
yt-dlp>=2024.12.0
spotipy>=2.24.0
python3-discogs-client>=2.7

# Metadata enrichment (Organize module)
musicbrainzngs>=0.7.1

# Recognition (Recognize module)
shazamio>=0.7.0
pydub>=0.25.1

# Background worker (jobs survive container restart)
huey>=2.5.1

# In-pipeline agent — enriches garbage-tagged metadata via Max subscription.
# Requires CLAUDE_CODE_OAUTH_TOKEN env (generated once via `claude setup-token`).
claude-agent-sdk>=0.1.0

# Cratemate module (album-cover identification + multi-platform enrichment).
# V2 (2026-05): recognition switched from Gemini to Claude vision via the
# claude-agent-sdk pinned above (Max OAuth, no API key, no per-call cost).
# torch/torchvision/google-cloud-vision/streamlit/google-generativeai stay
# deferred — Claude vision alone hits 6/7 perfect ID on a mixed test set
# (see /tmp/cratemate-test/claude_v2_results.json).
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.25.0
Pillow>=10.0.0
aiohttp>=3.9.0
# Perceptual hashing for Discogs cover-image verification — kills false
# positives like the Prefuse 73 hallucination (text-search hit that doesn't
# look anything like the upload). pHash + dHash combined, reject high-hamming.
imagehash>=4.3.0
# spotipy / musicbrainzngs / beautifulsoup4 / lxml / python3-discogs-client
# are already pinned above for wanted/organize — cratemate reuses them.
